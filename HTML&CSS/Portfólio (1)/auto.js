//Funcionalidade botão play para videos: -----------------------------
function playVideo() {
    var video = document.querySelector('video');
    if (video.paused) {
        video.play();
    } else {
        video.pause();
    }
}

function playVideo2() {
    var video = document.querySelector('.video2');
    if (video.paused) {
        video.play();
    } else {
        video.pause();
    }
}

function playVideo3() {
    var video = document.querySelector('.video3');
    if (video.paused) {
        video.play();
    } else {
        video.pause();
    }
}

function playVideo4() {
    var video = document.querySelector('.video4');
    if (video.paused) {
        video.play();
    } else {
        video.pause();
    }
}

function playVideo5() {
    var video = document.querySelector('.video5');
    if (video.paused) {
        video.play();
    } else {
        video.pause();
    }
}

function playVideo6() {
    var video = document.querySelector('.video6');
    if (video.paused) {
        video.play();
    } else {
        video.pause();
    }
}